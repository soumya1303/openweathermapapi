{
  "name": "weatherapi",
  "version": "1.0.0",
  "description": "basic app to make a API call to an external data service provider called openweathermap.org",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "som",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  }
}
